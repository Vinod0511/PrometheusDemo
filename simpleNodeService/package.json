{
  "name": "simplenodeservice",
  "version": "1.0.0",
  "description": "A sample node app returning success and failure responses on http requests and exposing prometheus metrics",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vinod Iyer",
  "license": "ISC",
  "dependencies": {
    "express": "4.17.1",
    "is-null-or-empty": "0.0.1",
    "prom-client": "11.5.3"
  }
}
